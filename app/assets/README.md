# About assets

## Images

- Use webp
- For each image generate a thumbnail (~20x20px) as placeholder source for fade in -> f.e. with https://squoosh.app/
  - Keep it below 4KB, so vite will server render it with base64 encoding
